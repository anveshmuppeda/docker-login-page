apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: team-alpha
  labels:
    app: webapp-sql
    tier: frontend
spec:
  selector:
    matchLabels:
      app: webapp-sql
      tier: frontend
  replicas: 1
  template:
    metadata:
      labels:
        app: webapp-sql
        tier: frontend
    spec:
      containers:
      - name: webapp1
        image: anvesh35/docker-login-frontend:phpv2
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: pass
        - name: MYSQL_DATABASE
          value: database
        - name: MYSQL_USER
          value: username
        - name: MYSQL_PASSWORD
          value: password
        - name: MYSQL_ROOT_HOST
          value: mysql-svc
        imagePullPolicy: Always
        ports:
        - containerPort: 80